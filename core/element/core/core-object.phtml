<object
    id="@{id}"
    name="@{name}"
    class="@{class}"
    form="@{form}"
    usemap="@{usemap}"
    typemustmatch="@{type|defaultype}"
    data="@{data}"
    type="@{type|defaultype}"
    width="@{width}"
    height="@{height}">
    ${child}
</object>